[project]
name = "comparehashes"
version = "0.1.0"
description = "Compare pHashes against built-in list."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "workers-py>=1.9",
    "workers-runtime-sdk"
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]